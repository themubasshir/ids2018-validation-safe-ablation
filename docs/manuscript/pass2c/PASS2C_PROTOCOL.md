# Pass 2C Reviewer-Readability Protocol

Status: **FROZEN before editorial editing**  
Branch: `manuscript-reproducibility-cleanup`  
Accepted baseline: `adcdb307c8ecb92e90269a0229f75a8986918c0c`  
Immutable Pass 2B candidate: `manuscript/manuscript_submission_candidate.md`  
Pass 2C working candidate: `manuscript/manuscript_submission_candidate_pass2c.md`  
Canonical bibliography: `manuscript/references.bib`

## Purpose

Pass 2C is a small reviewer-readability and framework-distillation pass. It may improve conceptual order, adoptability, sentence structure, and reviewer-facing presentation. It may not change scientific evidence, registered values, citation support, claim ceilings, evidence statuses, figure or table sources, or the Stage28 empirical wall.

## Frozen interventions

Only the following six editorial interventions are authorized:

1. reorder and simplify the abstract so the validation problem, framework, seed-stable development-regime finding, reference contrast, directional transfer, operational tension, and bounded thesis appear in that conceptual order;
2. convert Discussion 6.6 into a concise eight-item validation checklist with native evidence, controls, and qualifications;
3. reassess and likely simplify the title by comparing the two requested framework titles with the Pass 2B title;
4. consolidate six contributions into four without losing any supported substance;
5. compress the main-text presentation of 18 governed limitations into approximately six conceptual groups while preserving all 18 underlying limitations and claim ceilings;
6. simplify dense prose, especially in the Abstract, Discussion, Limitations, and Conclusion.

Reviewer-facing project-management metadata, including the manuscript-status block and Pass-development terminology, must be removed. Question-form Results headings must remain unchanged. The seven Pass 2B evidence tensions must all remain visible.

## Scientific prohibitions

No fitting, inference, scientific model loading or deserialization, target or holdout opening, statistic or metric generation, recalculation, confidence interval, plot, scientific table, threshold analysis, prevalence recomputation, LOAO or seed analysis, attribution analysis, profiling, dataset reconstruction, experiment, literature search, reference addition, evidence expansion, negative-finding removal, or registered-value alteration is permitted.

The two cross-dataset abstract values `NUM29-091` (`0.667483`) and `NUM29-094` (`0.108176`) are registered with six-decimal display rules. Pass 2C therefore must not shorten them to `0.67` and `0.11`, despite the optional readability suggestion. The temporal abstract values `NUM29-137` (`0.2599`) and `NUM29-141` (`0.6388`) retain their four-decimal meanings: mean PR-AUC for random-natural and chronological-natural development, respectively, on the shared forward target across the frozen five-seed linkage.

## Governance and outputs

All edits must resolve through the Pass 2B audits, Pass 2A citation registries, and Stage29 claim, number, evidence, limitation, figure, table, and prohibition registries. The Pass 2B manuscript remains untouched. Pass 2C must produce title, contribution, checklist, limitation, tension, thesis-alignment, diff, claim, number, and citation audits plus focused static tests and a final closeout.

The Pass 2C candidate may become the recommended submission baseline only after focused and full repository tests pass. No venue selection, LaTeX conversion, IEEE/Elsevier formatting, merge to `main`, submission, or new empirical stage is authorized.
