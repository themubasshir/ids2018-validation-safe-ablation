# Stage26 T4 Profiling Environment

The separate GPU receipt records a Tesla T4, driver 580.159.04, 15,360 MiB,
CUDA 12.8 and PyTorch 2.10.0+cu128. Other GPU-session package versions remain
`VERSION_NOT_PROVEN`.

This session must not be merged with the Stage26 CPU record or with Stage15,
Stage20 or Stage21 environments. Historical GPU timings remain archival and
are never rerun by verification tooling.
