"""Stage 19 protocol declaration; temporal materialization, fitting, inference, and holdout access are disabled."""

SOURCE_CELLS = tuple(range(290, 312))
CONFIG_PATH = "configs/stage19/protocol.json"
